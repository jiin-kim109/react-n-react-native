// Global Type Definitions
// Warning: beaware of globally typing each script's local properties. Only use if there are some that can apply to all, or to extend global types.

declare type ValueOf<T> = T[keyof T];
